version: '3.8'

services:
  bot:
    build: .
    container_name: doublekiss_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./doublekiss.db:/app/doublekiss.db
    environment:
      - PYTHONUNBUFFERED=1
